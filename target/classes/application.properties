spring.application.name=issue-tracker

# 1. Database Connection (Step 10: Use Env Variables for secrets)
spring.datasource.url=jdbc:mysql://localhost:3306/issue_tracker_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Subiksha@12345

# 2. Hibernate Settings
spring.jpa.hibernate.ddl-auto=update

# 3. Production Hardening: Disable SQL logs
spring.jpa.show-sql=false 

# 4. Standardizing Response (Step 9)
spring.web.error.include-message=always

# 5. Enable CORS for Frontend (Step 10)
# Allows your React app (usually on port 3000) to talk to this backend
app.cors.origins=http://localhost:3000